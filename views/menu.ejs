<!DOCTYPE html>
<html>
  <head>
    <title>Monthly Menu</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include('navbar') %>
    
    <main class="container">
      <h1>Monthly Menu - <%= month.charAt(0).toUpperCase() + month.slice(1) %> <%= year %></h1>
      
      <% if (noMenu) { %>
        <div class="no-menu-message">
          <p>No menu available for <%= month.charAt(0).toUpperCase() + month.slice(1) %> <%= year %>.</p>
          <p>Please check back later or select a different month.</p>
        </div>
      <% } else { %>
        <div class="monthly-menu">
          <% 
            // Group meals by date
            const mealsByDate = {};
            meals.forEach(m => {
              const dateObj = new Date(m.date);
              const dateStr = dateObj.toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' });
              if (!mealsByDate[dateStr]) {
                mealsByDate[dateStr] = [];
              }
              mealsByDate[dateStr].push(m);
            });
          %>
          
          <% Object.keys(mealsByDate).sort((a, b) => new Date(a) - new Date(b)).forEach(dateStr => { %>
            <div class="menu-day">
              <h3><%= dateStr %></h3>
              <div class="meals-for-day">
                <% mealsByDate[dateStr].forEach(m => { %>
                  <% if (m.meal) { %>
                    <div class="meal-card">
                      <a href="/meals/<%= m.meal._id %>">
                        <img src="<%= m.meal.img %>" alt="<%= m.meal.title %>" class="meal-thumbnail" />
                        <h4><%= m.meal.title %></h4>
                        <% if (m.meal.diet && m.meal.diet.length > 0) { %>
                          <p class="dietary-tags">
                            <% m.meal.diet.forEach(d => { %>
                              <span class="tag"><%= d %></span>
                            <% }); %>
                          </p>
                        <% } %>
                        <p class="view-details">Click to view details →</p>
                      </a>
                    </div>
                  <% } %>
                <% }); %>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </main>
  </body>
</html>
  </body>
</html>
